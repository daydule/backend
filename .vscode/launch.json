{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "attach",
            "port": 9229,
            "name": "Docker: attach to node_express",
            "address": "localhost",
            "restart": true,
            "localRoot": "${workspaceFolder}/app",
            "remoteRoot": "/daydule/app/",
            "skipFiles": ["<node_internals>/**"],
        }
    ]
}
